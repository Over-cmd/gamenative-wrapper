name: GameNative Vulkan Wrapper Build

on: [push, pull_request, workflow_dispatch]

jobs:
  build-gamenative-wrapper:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Core Source
        uses: actions/checkout@v4
        with:
          path: main-repo

      - name: Checkout Android libdrm Source
        uses: actions/checkout@v4
        with:
          repository: waydroid/android_external_libdrm
          ref: lineage-18.1
          path: main-repo/subprojects/libdrm

      - name: Checkout libadrenotools Source
        uses: actions/checkout@v4
        with:
          repository: Pipetto-crypto/libadrenotools
          ref: master
          path: main-repo/subprojects/libadrenotools

      - name: Fetch libadrenotools Git Submodules Recursively
        run: |
          cd main-repo/subprojects/libadrenotools
          echo "-> Inicializando submodulos de libadrenotools de forma recursiva..."
          git submodule update --init --recursive

      - name: Inject Hardware Patches for Mali-G52
        run: |
          echo "-> Aplicando parches de hardware en los archivos de utilidades..."
          find . -name "anon_file.c" -exec sed -i 's/fd = syscall(SYS_memfd_create.*/fd = memfd_create(debug_name, MFD_CLOEXEC \| MFD_ALLOW_SEALING);/g' {} + 2>/dev/null || true
          find main-repo/ -name "pan_device.c" -exec sed -i 's/pan_query_core_count(&dev->kmod.dev->props, &dev->core_id_range);/dev->core_id_range = pan_query_core_count(\&dev->kmod.dev->props);/g' {} + 2>/dev/null || true
          find main-repo/ -name "pan_device.c" -exec sed -i 's/\\&/\&/g' {} + 2>/dev/null || true
          
          echo "-> Purgando validaciones rigidas de librerias obsoletas en meson.build central..."
          find main-repo/ -name "meson.build" -exec sed -i "s/cc.find_library('rt'/dependency('', required : false) #/g" {} + 2>/dev/null || true
          find main-repo/ -name "meson.build" -exec sed -i "s/cc.find_library('atomic'/dependency('', required : false) #/g" {} + 2>/dev/null || true
          find main-repo/ -name "meson.build" -exec sed -i "s/cc.find_library('dl'/dependency('', required : false) #/g" {} + 2>/dev/null || true
          
          rm -f main-repo/subprojects/*.wrap main-repo/*.wrap 2>/dev/null || true

      - name: Install Base Build Tools
        run: |
          sudo apt-get update && sudo apt-get install -y build-essential libelf-dev bison flex pkg-config gettext patchelf unzip llvm clang
          python -m pip install --upgrade pip && pip install mako PyYAML 'meson>=1.4.0' ninja packaging

      - name: Auto-Detect True Android NDK Path
        id: ndk-detector
        run: |
          TRUE_NDK=""
          for path in /usr/local/lib/android/sdk/ndk/* /home/runner/Android/Sdk/ndk/*; do
            if [ -d "$path/toolchains/llvm/prebuilt/linux-x86_64/bin" ]; then TRUE_NDK="$path"; break; fi
          done
          echo "REAL_NDK_HOME=$TRUE_NDK" >> $GITHUB_ENV

      # 🟢 JUGADA MAESTRA EXTRAORDINARIA: Compilamos stubs binarios reales de Android con el Clang del NDK para burlar find_library() de forma legítima
      - name: Compile and Inject Real Binary Shims Stubs
        run: |
          cd main-repo
          unzip -o shims.zip -d ./
          mkdir -p "$GITHUB_WORKSPACE/shims_target"
          cp -rf ./shims/* "$GITHUB_WORKSPACE/shims_target/"
          
          echo "-> Creando codigo fuente dummy en C para simular funciones de Android..."
          echo "void __android_stub_placeholder(void) {}" > stub_android.c
          
          # Compilamos usando el Clang cruzado del propio NDK para que la arquitectura coincida al 100% con ARM64
          CLANG_CC="${{ env.REAL_NDK_HOME }}/toolchains/llvm/prebuilt/linux-x86_64/bin/aarch64-linux-android30-clang"
          LLVM_AR="${{ env.REAL_NDK_HOME }}/toolchains/llvm/prebuilt/linux-x86_64/bin/llvm-ar"
          
          $CLANG_CC -c stub_android.c -o stub_android.o
          
          echo "-> Empaquetando librerías .a binarias legítimas..."
          $LLVM_AR rcs "$GITHUB_WORKSPACE/shims_target/libandroid.a" stub_android.o
          $LLVM_AR rcs "$GITHUB_WORKSPACE/shims_target/liblog.a" stub_android.o
          $LLVM_AR rcs "$GITHUB_WORKSPACE/shims_target/libdl.a" stub_android.o
          $LLVM_AR rcs "$GITHUB_WORKSPACE/shims_target/libatomic.a" stub_android.o
          $LLVM_AR rcs "$GITHUB_WORKSPACE/shims_target/librt.a" stub_android.o
          
          echo "=== Pool de Shims Binarios Reales Verificado ==="
          ls -la "$GITHUB_WORKSPACE/shims_target"

      - name: Generate Indestructible Meson Cross-File
        run: |
          cd main-repo
          NDK_SYSROOT_LIB="${{ env.REAL_NDK_HOME }}/toolchains/llvm/prebuilt/linux-x86_64/sysroot/usr/lib/aarch64-linux-android/30"
          
          cat << EOF > cross.txt
          [constants]
          ndk_home = '${{ env.REAL_NDK_HOME }}'
          working_dir = '$GITHUB_WORKSPACE/main-repo'
          ndk_bin = ndk_home / 'toolchains/llvm/prebuilt/linux-x86_64/bin'
          ndk_sysroot = ndk_home / 'toolchains/llvm/prebuilt/linux-x86_64/sysroot'
          ndk_sysroot_lib = '${NDK_SYSROOT_LIB}'
          shims_path = '$GITHUB_WORKSPACE/shims_target'

          [binaries]
          c = [ndk_bin / 'clang', '-target', 'aarch64-linux-android30', '-D__TERMUX__']
          cpp = [ndk_bin / 'clang++', '-target', 'aarch64-linux-android30', '-fno-exceptions', '--start-no-unused-arguments', '--end-no-unused-arguments', '-D__TERMUX__']
          ar = ndk_bin / 'llvm-ar'
          strip = ndk_bin / 'llvm-strip'
          pkg-config = '/usr/bin/pkg-config'

          [host_machine]
          system = 'android'
          cpu_family = 'aarch64'
          cpu = 'aarch64'
          endian = 'little'

          [properties]
          sys_root = ndk_sysroot
          libdir = shims_path
          needs_exe_wrapper = true
          pkg_config_path = shims_path
          pkg_config_libdir = shims_path
          libdrm_path = '$GITHUB_WORKSPACE/main-repo/subprojects/libdrm'

          [built-in options]
          c_args = ['--sysroot=' + ndk_sysroot, '-fno-emulated-tls', '-I' + shims_path / 'include', '-isystem' + ndk_sysroot / 'usr/include', '-DHAVE_STRUCT_TIMESPEC', '-DHAVE_DLFCN_H', '-UHAVE_SECURE_GETENV', '-UHAVE_QSORT_S', '-include', 'fcntl.h', '-include', 'time.h', '-Wl,-llog', '-Wl,-lsync', '-fvisibility=default']
          cpp_args = ['--sysroot=' + ndk_sysroot, '-fno-emulated-tls', '-I' + shims_path / 'include', '-isystem' + ndk_sysroot / 'usr/include', '-DHAVE_STRUCT_TIMESPEC', '-DHAVE_DLFCN_H', '-UHAVE_SECURE_GETENV', '-UHAVE_QSORT_S', '-include', 'fcntl.h', '-include', 'time.h', '-include', 'dlfcn.h', '-Wl,-llog', '-Wl,-lsync', '-fvisibility=default', '-D_LIBCPP_ABI_NAMESPACE=__1']
          c_link_args = ['-landroid', '-llog', '-lsync', '-lhardware', '-L' + shims_path, '-L' + ndk_sysroot_lib, '--sysroot=' + ndk_sysroot, '-Wl,--allow-shlib-undefined', '-llog', '-lsync']
          cpp_link_args = ['-landroid', '-llog', '-lsync', '-lhardware', '-L' + shims_path, '-L' + ndk_sysroot_lib, '--sysroot=' + ndk_sysroot, '-Wl,--allow-shlib-undefined', '-llog', '-lsync']
          EOF

      - name: Configure GameNative Meson Setup
        run: |
          cd main-repo
          export ANDROID_NDK_HOME="${{ env.REAL_NDK_HOME }}"
          export MESON_WORKING_DIR="$GITHUB_WORKSPACE/main-repo"
          export PKG_CONFIG_PATH="$GITHUB_WORKSPACE/shims_target"
          export PKG_CONFIG_LIBDIR="$GITHUB_WORKSPACE/shims_target"
          
          echo "-> Lanzando inicialización de Meson para tu Mali-G52..."
          meson setup build --reconfigure --cross-file cross.txt --wrap-mode=nodownload -Dbuildtype=release -Dplatforms=android -Dandroid-stub=true -Dglx=disabled -Dgbm=disabled -Degl=disabled -Dopengl=false -Dgles1=disabled -Dgles2=disabled -Dllvm=disabled -Dvalgrind=disabled -Dzstd=disabled -Dvulkan-drivers=panfrost,wrapper -Dgallium-drivers=[]

      - name: Compile GameNative Engine
        run: |
          cd main-repo
          meson compile -C build

      - name: Structure and Pack wrapper.tzst for Bannerlator
        run: |
          cd main-repo
          rm -rf pkg && mkdir -p pkg/usr/lib pkg/usr/share/vulkan/icd.d pkg/usr/share/vulkan/settings.d
          cp -v build/src/vulkan/wrapper/libvulkan_wrapper.so pkg/usr/lib/libvulkan_wrapper.so 2>/dev/null || true
          cp -v build/src/panfrost/vulkan/libvulkan_panfrost.so pkg/usr/lib/libvulkan_panfrost.so 2>/dev/null || true
          
          patchelf --set-soname libvulkan_wrapper.so pkg/usr/lib/libvulkan_wrapper.so 2>/dev/null || true
          llvm-strip --strip-unneeded pkg/usr/lib/*.so 2>/dev/null || true
          
          echo '{"ICD": {"api_version": "1.4.352", "library_path": "libvulkan_wrapper.so"}, "file_format_version": "1.0.0"}' > pkg/usr/share/vulkan/icd.d/wrapper_icd.aarch64.json
          echo '{"ICD": {"api_version": "1.4.352", "library_path": "libvulkan_wrapper.so"}, "file_format_version": "1.0.0"}' > pkg/usr/share/vulkan/icd.d/wrapper_icd.arm.json
          echo '{"env":{"PAN_I_WANT_A_BROKEN_VULKAN_DRIVER":"1","MESA_VK_IGNORE_CONFORMANCE_WARNING":"1","PANVK_DEBUG":"sync,nir","MESA_VK_FORCE_BLIT":"1","MESA_LOADER_DRIVER_OVERRIDE":"panfrost","GALLIUM_DRIVER":"panfrost","vblank_mode":"0","MESA_GLSL_CACHE_DISABLE":"1","MESA_SHADER_CACHE_DISABLE":"true","NIR_DEBUG":"tgsi","MESA_VK_WSI_PRESENT_MODE":"immediate","MESA_VK_WSI_DEBUG":"always_async"}}' > pkg/usr/share/vulkan/settings.d/wrapper_settings.json
          
          echo "msf:315508" > pkg/version.txt && chmod -R 755 pkg/
          cd pkg && tar -I 'zstd -19 -T0' -cf ../../wrapper.tzst usr/ version.txt

      - name: Upload Final wrapper.tzst
        uses: actions/upload-artifact@v4
        with:
          name: GameNative-Mali-G52-Wrapper
          path: wrapper.tzst
